{% extends 'base.html.twig' %}

{% block title %}Catégories{% endblock %}

{% block body %}
    <div {{ vue_component('view/app/LabelPage', {
        'url': {
            'label': path('app_label')
        },
        'api': {
            'getLabels': path('api_label_get'),
            'update': path('api_label_update', {
                'id': 'id'
            }),
            'delete': path('api_label_remove', {
                'id': 'id'
            })
        }
    }) }}></div>
{% endblock %}